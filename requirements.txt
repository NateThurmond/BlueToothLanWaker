flask>=3.0.3
flask-socketio>=5.3.6
simple-websocket>=1.0.0
bleak>=0.22.3
# Linux/Pi only — enables detection of controllers that are paired with the Pi
# Requires system package first: sudo apt install libdbus-1-dev python3-dev
dbus-python>=1.3.2; sys_platform == "linux"
